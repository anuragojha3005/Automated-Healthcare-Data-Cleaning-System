MetadataExtractor.extract()

TypeInference.infer(column_name, series)

SemanticDetector.detect(column_name)

MixedTypeDetector.detect(df)

ProfilingEngine.profile(df)

Visualization.missing_heatmap(df, output)

Visualization.correlation_heatmap(df, output)

Visualization.outlier_plot(df, output)

RuleEngine.analyze(df)

ReportGenerator.save(report, output_file)